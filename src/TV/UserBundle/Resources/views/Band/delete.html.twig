{# src/TV/User/Resources/views/Band/delete.html.twig #}

{% extends "TVFindyourbandBundle::layout.html.twig" %}

{% block title %}
    {{ parent() }} - Supprimer groupe
{% endblock %}

{% block findyourband_body %}
    <div class="wrapper">
        <div class="container">
            <h2>Supprimer votre groupe</h2>
            <p>Etes-vous certain de vouloir supprimer cet groupe ?</p>

            <form action="{{ path('tv_band_delete', {'id': band.id}) }}" method="post">
                <a href="{{ path('tv_band_view', {'id': band.id}) }}" class="btn modify">
                    <i class="glyphicon glyphicon-chevron-left"></i>
                    Retour à l'annonce
                </a>

                <input type="submit" value="Supprimer" class="btn delete" />

                {{ form_rest(form) }}

            </form>         
        </div>
        <div class="clearfix"></div>
    </div>
{% endblock %}
